{% if error == false %}
<paymentAvisoResponse performedDatetime="{{ time }}" code="0" invoiceId="{{ invoiceId }}" shopId="{{ shopId }}"/>
{% else %}
<paymentAvisoResponse performedDatetime="{{ time }}" code="1" message="Заказ не найден"/>
{% endif %}