{% extends 'CrmMainBundle::layout_mini.html.twig' %}


{% block content %}

    {% if company is defined and company.id != 1013 %}
        {% if company.description == null %}
            <h1>{{ company.title }}</h1>
            <h2>подача карт для физических лиц:</h2>
        {% else %}
            {{ company.description | raw }}
        {% endif %}
        <br />
        <a href="{{ path('company-application-estr-add',{'url': company.url}) }}"><span class="btn">Карта ЕСТР</span></a>
        <br />
        <br />
        <a href="{{ path('company-application-ru-add',{'url': company.url}) }}"><span class="btn">Карта РФ</span></a>
        <br />
        <br />
    {% endif %}
    <a href="{{ path('company-application-skzi-add', {'url' : company.url}) }}"><span class="btn">Карта СКЗИ</span></a>

    <br />
    <br />
    <br />
    <br />
    <br />
    <br />
    <br />
    <br />
    <br />
    <br />
    <br />
    <br />
    <style>
        .btn{
            display: inline-block;
            width: 200px;
            text-align: center;
        }
    </style>
{% endblock %}
