{% extends url == null ? 'CrmMainBundle::layout.html.twig' : 'CrmMainBundle::layout_mini.html.twig' %}

{% block content %}
    <div class="box">
        <h2>Заявка создана, Номер заказа {{ order.id }}</h2>
        <br/>
        <br/>
        <a href="{{ path('usercompany_offer',{'orderId' : order.id, 'date' : order.created | date('dHmiY')}) }}">Скачать счет-заказ</a>
        <br/>
        <br/>
        <a href="{{ path('usercompany_payment_pdf',{'orderId' : order.id, 'date' : order.created | date('dHmiY')})) }}" target="_blank" class="btn">"Платежное поручение"</a><br/>
        Стоимость составит {{ order.price }} рублей ( НДС не облагается ). При условии оплаты в сбербанке комиссия составит приблизительно 70 рублей. Срок поступления денежных средств примерно 3-е суток.
    </div>
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        h3{
            margin: 10px 0;
        }
        .btn{
            padding: 8px 10px;
            display: inline-block;
            border-radius: 5px;
            background: #000;
            color: #FFF;
            margin-bottom: 10px;
            border: 0;
        }
        .btn:hover{
            box-shadow: 0 0 10px rgba(0,0,0,0.5);
            border-bottom: 0;

        }
    </style>

{% endblock %}