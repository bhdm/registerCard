{% if error == false %}
<checkOrderResponse performedDatetime="{{ time }}" code="0" invoiceId="{{ invoiceId }}" shopId="{{ shopId }}"/>
{% else %}
<checkOrderResponse performedDatetime="{{ time }}" code="100" invoiceId="{{ invoiceId }}" shopId="{{ shopId }}"
   message="Заказа не существует или он уже оплачен" techMessage="Заказа не существует или он уже оплачен"/>
{% endif %}
